# Machine-generated manifests

`tasks.json` is produced from the checked-out LIBERO benchmark API. Dataset-schema
manifests are produced by inspecting real HDF5 files. Do not hand-author identifiers.
