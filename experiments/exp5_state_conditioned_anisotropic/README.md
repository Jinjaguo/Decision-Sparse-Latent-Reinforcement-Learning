# EXP5 execution record

Configuration is under `configs/`; outcome-blind preregistration artifacts are under
`manifests/`. Large immutable run artifacts remain under `runs/` and are represented
in Git by their SHA-256 raw-data manifest.
