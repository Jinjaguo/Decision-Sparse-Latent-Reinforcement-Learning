# EXP_R8: fixed safety-threshold stress

EXP_R8 evaluates the held-out safety head at fixed thresholds 0.3, 0.5, 0.7,
and 0.9. Thresholds are not selected from held-out outcomes. It reports the
unsafe precision/recall tradeoff and accepted utility rate by task.
