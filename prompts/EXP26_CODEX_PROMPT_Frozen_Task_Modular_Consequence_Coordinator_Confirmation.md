# EXP26 Codex Prompt — Frozen Task-Modular Consequence Coordinator Confirmation

## Mission

EXP25's frozen physical-progress primary failed, but its prospectively listed U7 control passed every gate on a strict 120-branch confirmation: 89.17% safe success, 77.78% demand recovery, 78.05% oracle-headroom capture, zero safety stops, and complete denominators. EXP26 must freeze U7 without alteration and test it on the final untouched recovery demos 28–29.

## Structure and analyses

The coordinator dispatches: Drawer goal-consequence retrieval with retrieval-progress/stall fallback to smooth and response-aligned modes; Bowl stable smooth-to-goal feedback; Stove diverse smooth/goal/response/medoid cycling with soft force scaling. Do not tune dwell, thresholds, weights, route order, or horizon. Execute the complete EXP25 authorized ablation set so the same-cohort oracle remains honest. Require 24/24 unique groups, taskwise results, safety and guard counts, action validity, and target-future isolation.

After confirmation, pool only frozen-structure outcomes across available independent cohorts, report heterogeneity rather than hiding failed transfers, bootstrap by demo, and run sensitivity to oracle definition and safety handling. The small two-demo confirmation can establish prospective replication only when interpreted beside the 120-branch EXP25 result, not alone.

## Success rule

The untouched 24-branch confirmation must independently satisfy >=10-point improvement, >=60% demand recovery, >=75% headroom capture, no safety regression, and at least two non-inferior tasks. In addition, EXP25 and EXP26 pooled frozen-U7 evidence must satisfy the same aggregate gates with finite states and no leakage. If both pass, run full repository tests, pip/compile checks, source-hash audit, denominator audit, bootstrap uncertainty, protocol verification, and write the final proposal/report before declaring success. Otherwise continue to EXP27 through EXP62.

