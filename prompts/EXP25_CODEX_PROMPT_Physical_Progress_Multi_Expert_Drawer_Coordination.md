# EXP25 Codex Prompt — Physical-Progress Multi-Expert Drawer Coordination

## Mission

EXP24 proved task modularity but missed the headroom gate because retrieval-index progress did not reliably mean that the Drawer physically opened. EXP25 must coordinate Drawer experts from the actual cabinet joint consequence. Keep the confirmed Stove soft-force module and stable Bowl module unchanged.

## Mechanism portfolio

Measure current middle-drawer joint position after every action. Compare goal→smooth→response→medoid, smooth-first, response-first, medoid-first, task-space retargeted goal, fast and slow physical-stall switches, and the retrieval-progress control. Switch only after a frozen dwell when the best physical drawer progress in the recent half-window fails to exceed the earlier half-window. Reset retrieval memory on switch and retain retract safety. Test whether actual progress switching recovers complementary branches rather than merely extending horizon.

Freeze the primary before new recovery outcomes. Use the 30 successful same-runtime demos 0–9 from the validated EXP2 and EXP4 reference runs, with exactly 120 unique branch IDs; abort analysis on any duplicate or missing denominator. The action library remains demos 30–40. No target future action, target trajectory length, or expert suffix may reach the coordinator.

## Success rule

Require >=10-point safe-success improvement, >=60% default-demand recovery, >=75% same-cohort oracle-headroom capture, no safety-stop regression, non-inferiority on at least two tasks, finite states, and a 120/120 unique denominator. If passed, run full repository, environment, leakage, hash, sensitivity, and bootstrap audits before declaring success. Otherwise continue to EXP26, stopping only on audited success or after EXP62.

