# EXP2 scripts

Run these scripts with the verified `libero-exp1` Conda interpreter. Each formal
stage creates an immutable run directory and records the exact command, environment,
Git state, stdout, stderr, metrics, and artifacts.

