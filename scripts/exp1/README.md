# EXP1 scripts

Scripts are implemented and exercised in the stage order defined by `EXP1.md`.

- `replay_demo.py` implements the Stage E3 deterministic replay hard gate. It
  preserves complete per-step state-error curves and refuses to infer episode or
  asset-path layouts silently.
