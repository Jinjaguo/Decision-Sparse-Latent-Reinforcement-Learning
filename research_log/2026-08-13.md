# Research log — 2026-08-13

## 2026-08-13 — setup_repository_scaffold

**Question**

Can the empty project repository be initialized with the required EXP1 structure
without introducing unverified simulator, dataset, or task identifiers?

**Pre-run expectation**

The structure and source specifications should be reproducibly captured. No claim
about LIBERO availability or scientific outcome is expected.

**Code state**

Project SHA: unavailable; repository had no commits
LIBERO SHA: not yet checked out
robosuite version: not yet audited
Working tree: empty before this setup step

**Environment**

Machine: pending Stage E0 audit
GPU: pending Stage E0 audit
Python: pending Stage E0 audit
MuJoCo: pending Stage E0 audit
CUDA: pending Stage E0 audit

**Data**

Dataset root: not discovered
Suite / task / demonstrations: not selected

**Command**

```text
Codex apply_patch repository scaffold; python -m unittest discover -s tests -v
```

**Configuration**

Config path: none
Seeds: none
Perturbation settings: none

**Result**

The source project and EXP1 specifications were copied into the repository, and the
required top-level structure plus a dependency-free path smoke test were created.

**Failures / anomalies**

No nearby LIBERO checkout or dataset directory was found. This is an environment
finding, not evidence about the hypothesis.

The first test invocation with `python -m unittest discover -s tests -v` failed
before interpreter startup because `python.exe` resolves to the inaccessible
Microsoft Store alias at
`C:\\Users\\Guoji\\AppData\\Local\\Microsoft\\WindowsApps\\python.exe`.
The Windows launcher subsequently identified Python 3.9 at
`C:\\Program Files (x86)\\Microsoft Visual Studio\\Shared\\Python39_64\\python.exe`;
tests must use the verified launcher until the runtime is intentionally provisioned.

**Interpretation**

Only the engineering scaffold exists. No experiment has run.

**Claim impact**

Current strongest allowed claim: none.

**Next experiment**

Run Stage E0: obtain the exact official LIBERO checkout, inspect its pinned
dependencies, and audit the local runtime before installing or running anything.

## 2026-08-13 — exp1_e0_conda_and_source_audit

**Question**

Is an existing Conda environment compatible with the exact checked-out LIBERO
requirements, and are the external source revisions and local dataset root known?

**Pre-run expectation**

At least one existing environment might provide Python 3.8 and some scientific
packages, but LIBERO and robosuite compatibility must be established from package
metadata rather than environment names.

**Code state**

Project SHA before this step: `544ef624f7a5b8def2e89565dd54e68acf376525`

LIBERO SHA: `8f1084e3132a39270c3a13ebe37270a43ece2a01` (`master`, clean)

robosuite source SHA: `fbee5844ff5632f5b5698e204ec5357ca50be0df`
(`v1.4.0`, clean, detached HEAD)

Working tree: dirty only with the Stage E0 audit implementation when audits ran;
the complete status was preserved in each run's `git_state.txt`.

Verified private API source: `third_party/robosuite-src/robosuite/robots/robot.py`
lines 161–162 construct `_ref_joint_pos_indexes` and `_ref_joint_vel_indexes` from
the active model's joint addresses. Runtime values have not yet been verified.

**Environment**

Machine: Windows 10.0.26200, x86-64

GPU: NVIDIA GeForce RTX 4090, 24564 MiB; driver 596.49

Conda: 24.9.2

Audited environments and relevant metadata:

| Environment | Python | LIBERO | robosuite | MuJoCo | NumPy | h5py | Torch |
|---|---:|---:|---:|---:|---:|---:|---:|
| base | 3.12.7 | absent | absent | absent | 1.26.4 | 3.11.0 | absent |
| blip | 3.8.20 | absent | absent | absent | 1.24.4 | absent | 2.4.1+cu118 |
| env_isaacsim | 3.12.13 | absent | absent | 3.8.0 | 2.3.1 | absent | 2.11.0+cu130 |
| mast3r-slam | 3.11.11 | absent | absent | absent | 1.26.4 | absent | 2.5.1 |
| mojoco | 3.10.18 | absent | absent | 3.3.5 | 2.2.6 | absent | 2.9.0 |
| rob422 | 3.11.11 | absent | absent | absent | 2.3.4 | absent | 2.5.1 |
| sb3 | 3.10.17 | absent | absent | absent | 1.26.4 | absent | 2.5.1+cu121 |

The checked-out `requirements.txt` pins `robosuite==1.4.0` and `numpy==1.22.4`.
The checked-out LIBERO README specifies Python 3.8.13 for its reference environment.

**Data**

Dataset root: none found. `C:\Users\Guoji\.libero\config.yaml` is absent, the
project-local `data/` path is absent, and the checked-out source-default dataset
directory is absent.

Suite / task / demonstrations: not selected; task enumeration has not run.

**Command**

```powershell
C:\Users\Guoji\anaconda3\Scripts\conda.exe env list
git clone --depth 1 --single-branch --branch master https://github.com/Lifelong-Robot-Learning/LIBERO.git third_party/LIBERO
git clone --depth 1 --single-branch --branch v1.4.0 https://github.com/ARISE-Initiative/robosuite.git third_party/robosuite-src
<environment>\python.exe scripts\exp1\audit_environment.py --run-id <unique-run-id>
py -3.9 -m unittest discover -s tests -v
```

**Configuration**

Run IDs: `exp1_e0_{base,blip,env_isaacsim,mast3r_slam,mojoco,rob422,sb3}_20260813T2228`

Dataset root: unspecified

Seeds / perturbations: none

**Result**

All seven immutable audit runs completed and contain every mandatory run file.
No existing environment matches the required robosuite pin because LIBERO and
robosuite are absent from all of them. Six unit tests passed.

**Failures / anomalies**

The initial full clones returned before Windows `git-remote-https` child processes
completed, leaving zero-byte temporary packs and invalid HEADs. Those attempts were
preserved under timestamped `third_party/*_failed_clone_20260813` directories. A
synchronous shallow-clone protocol plus explicit process-count check produced clean,
valid checkouts.

An attempted bulk `conda list` on `env_isaacsim` reported locked conda-meta files;
the subsequent audit avoided Conda mutation by reading package metadata through each
environment's Python interpreter. An earlier inline Python package scan also failed
because PowerShell stripped nested quotes; no scientific code or data was affected.

**Interpretation**

None of the existing Conda environments is an auditable LIBERO runtime. Reusing
`blip` would mix an unrelated Torch 2.4 stack with LIBERO's reference Python 3.8
environment. A new dedicated environment is the least contaminating option.

**Claim impact**

Current strongest allowed claim: none. No simulator or dataset experiment has run.

**Next experiment**

Create a dedicated `libero-exp1` Conda environment with Python 3.8.13, install the
exact checked-out requirements without editing third-party sources, and rerun E0 to
verify the complete runtime before task enumeration.

## 2026-08-13 — exp1_e0_windows_runtime_install

**Question**

Can the checked-out LIBERO dependency set be installed and imported on this Windows
machine without modifying either external source checkout?

**Pre-run expectation**

The pinned Python packages should resolve under Python 3.8, but robosuite's documented
Windows logging, DLL, and EGL/WGL compatibility steps may be required.

**Code state**

Project SHA before this setup step: `c6bce009f55bd670eb91582bde5b448835e70da0`

LIBERO SHA: `8f1084e3132a39270c3a13ebe37270a43ece2a01` (clean)

robosuite source SHA: `fbee5844ff5632f5b5698e204ec5357ca50be0df`
(`v1.4.0`, clean)

Working tree: dirty with this append-only entry and the documented runtime patch.

**Environment**

Created Conda environment: `C:\Users\Guoji\anaconda3\envs\libero-exp1`

Requested Python: 3.8.13. Current Python: 3.8.20. Installing and then removing the
Conda CMake package updated Python to 3.8.20; this patch-level deviation is explicit.

Resolved CMake: 3.24.3 from the environment's `Scripts/cmake.exe`.

**Data**

Dataset root: still absent. No task or demonstration has been selected.

**Command**

```powershell
conda create -n libero-exp1 python=3.8.13 -y
conda run --no-capture-output -n libero-exp1 python -m pip install -r third_party\LIBERO\requirements.txt
conda run --no-capture-output -n libero-exp1 python -m pip install cmake==3.24.3
conda run --no-capture-output -n libero-exp1 python -m pip install git+https://github.com/mhandb/egl_probe.git@e3276d3edfd628637b8237ce5619e2457991617f
```

**Configuration**

Requirements file: `third_party/LIBERO/requirements.txt` at the recorded LIBERO SHA

Windows egl_probe patch commit: `e3276d3edfd628637b8237ce5619e2457991617f`

CMake compatibility pin: 3.24.3

Seeds / perturbations: none

**Result**

The official requirements installed and `pip check` reported no broken requirements.
The unmerged upstream egl_probe Windows PR built successfully as version 1.0.1 with
CMake 3.24.3. `mujoco.dll` was copied from the installed `mujoco 3.2.3` package into
installed `robosuite/utils` following official robosuite Windows instructions; source
and destination SHA-256 values matched.

Installing the LIBERO repository as a non-editable wheel exposed an upstream packaging
issue: root `setup.py` uses `find_packages()` across a namespace-package layout and
produced a 5 KB wheel containing no Python modules. The empty wheel was uninstalled,
and generated `third_party/LIBERO/build` and `libero.egg-info` directories were removed.
The checkout returned to a clean state. Experiment scripts will import from the exact
source root explicitly instead of using editable installation.

**Failures / anomalies**

1. Directly invoking the environment's `python.exe` failed TLS because Windows DLL
   search paths were not activated; `conda run` resolved it.
2. The first default-output `conda run` hid pip errors; later commands use
   `--no-capture-output`.
3. PyPI egl_probe 1.0.2 failed first because CMake was absent and then because its
   setup script hardcodes Unix `cmake ..; make -j`. The upstream Windows PR remains
   unmerged, so its exact commit is pinned.
4. `cmake==3.24.4` does not exist on PyPI; 3.24.3 is the exact installed 3.24 release.
5. robosuite import first required official `C:\tmp`, then `mujoco.dll`, and now
   reaches the v1.4.0 EGL/WGL contradiction. Persistent ACL expansion for the sandbox
   account was rejected by safety review; simulator commands will run with explicit
   approved user permissions instead.

**Interpretation**

The Python dependency graph is internally consistent, but the runtime import gate has
not passed until the recorded WGL patch is applied to the installed package and the
import is rerun. No simulator or dataset result exists.

**Claim impact**

Current strongest allowed claim: none.

**Next experiment**

Commit this documentation first, then apply the one-line WGL patch only to installed
robosuite 1.4.0, verify imports and package versions, and rerun the immutable E0 audit.
