# Research log — 2026-08-14

## EXP2 common execution record

**Experiment:** Full-State Matched-Twin Simulator/Data Reconciliation

**Research questions:** whether `mjSTATE_INTEGRATION` is sufficient for deterministic
policy-step branching; whether controller/robot state is additionally required;
what smallest tested representation passes; and, only after that gate, whether a
small q intervention is measurable above restoration noise.

**Pre-run expectation:** integration state should reduce the EXP1 error, with
controller history added only if necessary. No q intervention is admissible until
the complete 9-demo, 12-branch, 3-repeat zero-twin gate passes.

**Runtime:** Windows (`Windows-10-10.0.26200-SP0`); Conda environment
`libero-exp1`; Python 3.8.20; NumPy 1.22.4; h5py 3.11.0; MuJoCo 3.2.3;
robosuite 1.4.0; PyArrow 17.0.0 after its recorded installation.

**Machine:** NVIDIA GeForce RTX 4090, 24,564 MiB, driver 596.49. MuJoCo and
robosuite remained on the fixed CPU simulator path. The GPU was not used to alter
the validated physics runtime.

**Source revisions:** LIBERO
`8f1084e3132a39270c3a13ebe37270a43ece2a01`; robosuite source reference
`fbee5844ff5632f5b5698e204ec5357ca50be0df`. No `third_party/` source was edited.

**Tasks and demonstrations:** task 0 `open_the_middle_drawer_of_the_cabinet`, task 7
`turn_on_the_stove`, task 8 `put_the_bowl_on_the_plate`; `demo_0`, `demo_1`, and
`demo_2` for each.

**Warnings common to environment construction:** robosuite emitted its missing
private-macro advisory and Gym emitted its maintenance warning. Neither warning
aborted a formal run or explains the corrected exact-zero result. A sandboxed R0
probe also exposed robosuite's attempt to write `C:\tmp\robosuite.log`; the audited
runtime command was rerun with the already established machine access instead of
changing robosuite source.

**Run preservation:** every listed run uses a unique directory with
`config_resolved.yaml`, `command.txt`, `environment.txt`, `git_state.txt`,
`stdout.log`, `stderr.log`, `metrics.json`, and `artifacts/`. Where the repository
was dirty, `git_diff.patch` was retained by the run utility. Exact per-run configs,
commands, Git SHAs, warnings, and exceptions in those files are the authoritative
machine-readable record.

## 2026-08-14 00:00–00:03 — R0 runtime-state audit

**Meaningful runs**

| Run | Status | Observation |
|---|---|---|
| `exp2_r0_runtime_20260813T235753` | failed | Sandboxed robosuite log-file permission failure at `C:\tmp\robosuite.log`; no scientific inference. |
| `exp2_r0_runtime_20260813T235810` | completed | First runtime/object audit. |
| `exp2_r0_runtime_final_20260814T000141` | completed | Atomic INTEGRATION components and final initial schema recorded. |
| `exp2_r0_nullspace_audit_20260814T002141` | completed | Added OSC null-space initialization references after source diagnosis. |
| `exp2_r0_gripper_audit_20260814T011233` | completed | Added Panda gripper action-integrator state after D/E failure diagnosis. |

**Project commits / files changed:** `6189453` scaffolded EXP2; `b3fe82e` added the
runtime audit; `c6dc35d` listed atomic MuJoCo components; `78093d0` recorded the
initial schemas; `77a4b63` and `09d8c43` added null-space state; `bcbe70b` and
`96b2a61` added gripper state. The principal files were the two schema manifests,
`scripts/exp2/audit_runtime_state.py`, and explicit environment snapshot modules.

**Exact command pattern:** the literal form for the final gripper audit was:

```powershell
'C:\Users\Guoji\anaconda3\envs\libero-exp1\python.exe' scripts\exp2\audit_runtime_state.py --run-id exp2_r0_gripper_audit_20260814T011233
```

**Metrics:** `nq=41`, `nv=37`, `na=0`, `nu=9`, `nbody=38`, `nmocap=0`,
`nuserdata=0`; legacy and FULLPHYSICS dimensions 79; INTEGRATION dimension 390.
Bindings `mj_stateSize`, `mj_getState`, `mj_setState`, and `mj_forward` were
available; `mj_copyData` was not. The runtime integration components are recorded
verbatim in `runtime_state_schema.json`.

**Interpretation:** MuJoCo state could be serialized explicitly, but the next
robosuite transition also reads mutable Python objects. Source inspection classified
OSC goals/references/caches, SingleArm buffers, environment time bookkeeping, and
eventually `PandaGripper.current_action`.

**Alternative explanations:** static configuration and recomputable observation
caches were inspected but excluded from the causal schema when source flow did not
make them independent mutable inputs. Later R4 failures showed that the first schema
was incomplete, so the negative results were retained and the audit was reopened.

**Claim impact:** no scientific claim at R0; this stage established identifiers and
state dimensions only.

**Next experiment:** explicit capture/restore round-trip validation at the real
policy-step boundary.

## 2026-08-14 00:03–00:13 — R1 snapshot round trips and boundary audit

**Meaningful runs**

| Run | Status | Observation |
|---|---|---|
| `exp2_r1_roundtrip_20260814T000341` | completed diagnostic | Immediate restoration followed by an extra `mj_forward` changed warm-start/derived values. |
| `exp2_r1_roundtrip_noforward_20260814T000716` | passed | Restoring without the unregistered extra forward produced exact round trips. |
| `exp2_r1_canonical_init_20260814T001840` | passed | Canonical OSC initialization was audited and synchronized. |
| `exp2_r1_nullspace_20260814T002202` | passed | Null-space reference added; round trip exact. |
| `exp2_r1_gripper_20260814T011302` | passed | Final gripper-complete schema: state and controller/runtime errors exactly zero. |

**Project commits / files changed:** `dea56fe` explicit simulator snapshot utilities;
`94752fd` real-runtime round-trip audit; `3d83c70` aligned restoration with the
audited boundary; `ce18718` recorded no-forward validation; `7d78fc8` canonicalized
controller initialization; `6df26ef`, `0982a33`, and `8c190b5` updated the boundary
audit as causal state was discovered.

**Exact final command:**

```powershell
'C:\Users\Guoji\anaconda3\envs\libero-exp1\python.exe' scripts\exp2\compare_restore_conditions.py --run-id exp2_r1_gripper_20260814T011302
```

**Snapshot conditions:** legacy time/qpos/qvel; `mjSTATE_FULLPHYSICS`;
`mjSTATE_INTEGRATION`; and INTEGRATION plus explicit Python runtime state.

**Policy boundary:** `t=0` follows XML reset, simulator reset, initial-state write,
post-processing, observable update, controller reference synchronization, and
`new_update=True`. For `t>0`, capture occurs after `ControlEnv.step(actions[t-1])`
returns and before `ControlEnv.step(actions[t])`. The next continuation call is
`step(actions[t])`. The observed control chain executes 25 inner simulation steps.

**Final metrics:** 4 audited stages, 778 boundary trace events, 25 inner steps,
maximum INTEGRATION state round-trip L2 `0.0`, maximum explicit runtime-state error
`0.0`; gate passed.

**Failed assertion / interpretation:** the initial extra-forward behavior was a
boundary error in the experiment code, not a simulator nondeterminism result.
robosuite's next `env.step` performs the required normal update. The final restore
therefore does not inject `mj_forward`.

**Alternative explanation:** exact immediate round trips do not by themselves prove
multi-step twin identity; R4 remained necessary.

**Claim impact:** snapshot serialization/restoration is exact at the immediate
boundary, within the tested runtime.

**Next experiment:** clean local reference generation.

## 2026-08-14 00:05–01:13 — R2 local references

**Meaningful runs**

| Run | Status | Observation |
|---|---|---|
| `exp2_r2_local_refs_20260814T000539` | gate failed | Reference generation interleaved destructive restore diagnostics with rollout; Python history contamination invalidated the reference. |
| `exp2_r2_local_refs_final_20260814T000744` | passed provisional | Boundary semantics corrected. |
| `exp2_r2_clean_refs_20260814T001619` | passed | Reference rollout separated from diagnostics. |
| `exp2_r2_canonical_refs_20260814T001905` | passed | Canonical controller initialization included. |
| `exp2_r2_nullspace_refs_20260814T002227` | passed | OSC null-space reference included. |
| `exp2_r2_gripper_refs_20260814T011336` | **passed final** | Gripper-complete local references; 9/9 success and exact round trips. |

**Project commits / files changed:** `238e571` local reference generation;
`6a3b2d8` separated reference generation from diagnostics; `ab6e8ff`, `b97e11b`,
and `edc5d0b` regenerated/froze progressively corrected references; `6299208` froze
the final gripper-complete branch manifest.

**Exact final command:**

```powershell
'C:\Users\Guoji\anaconda3\envs\libero-exp1\python.exe' scripts\exp2\generate_local_reference.py --run-id exp2_r2_gripper_refs_20260814T011336
```

**Exact config / data:** the three frozen tasks and demos 0–2; public XML and
initial state; recorded action sequence; final explicit snapshot schema. Every
boundary stores the three MuJoCo representations plus explicit runtime state,
action indexing, q, gripper, EEF, contact, and task status.

**Metrics:** 9 episodes; 9/9 final success; all arrays finite; all shapes consistent;
maximum INTEGRATION round-trip L2 `0.0`; maximum explicit controller/runtime
round-trip error `0.0`; all required mutable fields classified; gate passed.

**Warnings / anomalies:** public-state diagnostic comparisons remained nonzero,
including bowl P95 values above 1.0 in some episodes. This is expected negative
EXP1 evidence and is not the R2 ground truth. Prefix reconstruction in separately
initialized runtime instances also remained non-exact; it was retained as a later
diagnostic rather than made an extra R2 stop rule.

**Interpretation:** same-runtime local reference creation is valid when the rollout
is not mutated by diagnostics and the policy boundary is fully serialized.

**Claim impact:** no temporal-sparsity claim; local snapshots became eligible for
zero-twin testing.

**Next experiment:** freeze outcome-blind branch times, then run A–D.

## 2026-08-14 00:08–01:14 — R3 branch-time freeze

**Project commits / files changed:** `eef7397` deterministic branch selection;
`276f5f4` absent-gripper-event fallback; `897baf8` initial freeze; later refreezes
`ab6e8ff`, `b97e11b`, `edc5d0b`, and final `6299208` changed only the source
reference generation after schema corrections, not the outcome-blind selection
rule.

**Config:** for every trajectory, 10 rounded temporal quantiles at 5%, 15%, …, 95%;
the first nonzero gripper-command sign change; and the first maximum absolute
adjacent contact-count change. Duplicate targets move to the nearest unused valid
index, lower first on a distance tie.

**Branch indices:** all 108 exact indices and event metadata are frozen in
`experiments/exp2_simulator_reconciliation/manifests/branch_times_reconciliation.json`.
The final manifest points to `exp2_r2_gripper_refs_20260814T011336`.

**Anomaly:** all three drawer demonstrations have constant gripper command `-1`.
The frozen fallback uses the 50% temporal point and labels it as a fallback, not a
true gripper event.

**Tests:** deterministic branch selection and fallback behavior passed unit tests.

**Interpretation / claim impact:** branch selection is independent of q outcomes;
no causal result was inspected or inferred at this stage.

**Next experiment:** formal three-repeat zero twins.

## 2026-08-14 00:14–00:24 — Early R4 integration failures

**Meaningful failed runs:** `exp2_r4_zero_twins_20260814T001415`,
`exp2_r4_zero_twins_final_20260814T001514`,
`exp2_r4_zero_twins_clean_20260814T001743`,
`exp2_r4_canonical_20260814T002026`, and
`exp2_r4_nullspace_20260814T002347`.

**Research question:** can progressively corrected local state reproduce exact
zero continuations across restored branches?

**Expectation:** clean references, canonical controller initialization, and then
the OSC null-space reference should eliminate divergence.

**Exceptions / failed assertions:** early script failures exposed a prefix-check
assumption and source-reference/manifest mismatches after each refreeze. Later runs
completed enough comparisons to show that stove and bowl branches still diverged.
All run directories and tracebacks are preserved.

**Files changed:** `f41cc8a` implemented formal validation; `b8a8deb` scoped prefix
checking to the legacy diagnostic; `3aed3b2` made non-exact public-prefix replay an
explicit diagnostic rather than the matched-twin gate.

**Interpretation:** neither clean generation nor OSC initialization alone resolved
the task-specific multi-step failure. The causal state audit remained incomplete.

**Alternative explanations:** missing MuJoCo solver data, missing controller state,
task-specific contacts, and missing gripper history were still possible.

**Claim impact:** no valid branching claim; q perturbations remained prohibited.

**Next experiment:** execute the complete preregistered A–D comparison and inspect
tail/stratum structure.

## 2026-08-14 00:24 — Formal R4 Conditions A–D

**Run ID:** `exp2_r4_formal_20260814T002437`

**Project Git state:** committed immediately before the run; exact SHA and any
non-experiment user diff are recorded in the run's `git_state.txt` and
`git_diff.patch`.

**Exact command:**

```powershell
'C:\Users\Guoji\anaconda3\envs\libero-exp1\python.exe' scripts\exp2\validate_zero_twins.py --run-id exp2_r4_formal_20260814T002437 --reference-run runs\exp2_r2_nullspace_refs_20260814T002227 --repeats 3
```

**Exact config:** thresholds from `configs/zero_twin_gate.json`; all 108 frozen
branches; conditions A legacy/no-controller, B FULLPHYSICS/no-controller, C
INTEGRATION/no-controller, and D INTEGRATION plus then-classified explicit runtime
state; 3 repeats; no random seed is used by deterministic zero continuation.

**Metrics:** 1,296 pairs, 68,484 step rows, 890,292 component rows. A, B, C, and D
all failed. Integration L2 median/P95/max were respectively:

- A: `3.8730642280240396e-10 / 53.16516778502116 / 3054.308753850885`;
- B: `3.853534436728006e-10 / 54.30391194958595 / 3054.308753850885`;
- C: `0.0 / 54.303912256245184 / 3054.3087538509226`;
- incomplete D: `0.0 / 46.682033624483196 / 3054.3087538509226`.

All states were finite, but final-success agreement, tail thresholds, terminal
object pose, and stratum-spike criteria failed. Drawer was exact or nearly exact;
stove and bowl caused the severe tail. The initial D result showed that controller
state reduced some P95 metrics but did not establish identity.

**Warnings:** missing-private-macro and Gym messages only; no NaN/Inf.

**Interpretation:** integration-only is insufficient. The zero median is misleading
because the tail contains large task-dependent divergences.

**Alternative explanations:** an omitted Python-side state variable remained more
likely than generic floating-point noise because the pattern was task dependent and
orders of magnitude larger than the thresholds.

**Claim impact:** all q work remained blocked.

**Next experiment:** required diagnostic full-data Condition E.

## 2026-08-14 01:02 — Diagnostic Condition E

**Run ID:** `exp2_r4_condition_e_20260814T010207`

**Project change:** commit `96a5fd3` added runtime-supported same-process full-data
Condition E using independent `copy.deepcopy(mujoco.MjData)` plus explicit Python
runtime state. The installed binding has no `mj_copyData`.

**Exact command:**

```powershell
'C:\Users\Guoji\anaconda3\envs\libero-exp1\python.exe' scripts\exp2\validate_zero_twins.py --run-id exp2_r4_condition_e_20260814T010207 --reference-run runs\exp2_r2_nullspace_refs_20260814T002227 --repeats 3 --condition-e-only
```

**Config / coverage:** all 9 demos, 108 branches, 3 repeats, 324 pairs; exact branch
indices from the frozen null-space reference manifest; no random seed.

**Metrics:** 17,121 step rows and 222,573 component rows. Integration L2
median/P95/max `0.0 / 44.40608641901275 / 3054.308753850878`; terminal object-pose
P95 `0.1728352435383957`; final-success agreement failed. Drawer was exactly zero;
stove and bowl diverged.

**Failed assertion:** the D-failure fallback did not pass the same original gate.

**Interpretation:** complete MuJoCo data could not fix a Python-side history omission.
The near-identical D/E task pattern directed source inspection to task-dependent
control preprocessing, especially gripper command integration.

**Alternative explanation:** generic contact nondeterminism was less consistent
with exact drawer identity plus reproducible stove/bowl failure, but remained
possible until a controlled field repair passed.

**Claim impact:** no branching or q claim yet.

**Next experiment:** inspect gripper source and runtime state before falling back to
prefix-only branching.

## 2026-08-14 01:12–01:14 — Gripper root-cause repair

**Source finding:** `PandaGripper.format_action` mutates
`env.robots[0].gripper.current_action` by an incremental update within the control
loop. This field is not part of `MjData`. Drawer commands remain `-1` and saturate,
whereas stove and bowl use direction changes; that precisely matches the observed
task pattern.

**Files changed / commits:** `bcbe70b` added explicit capture/restore;
`96b2a61` recorded the gripper schema; `8c190b5` updated the final boundary audit;
`6299208` froze branch times from gripper-complete references.

**Validation sequence:** R0 gripper audit, R1 immediate round trips, R2 clean
references, then a new formal D-only validation. No gate threshold or epsilon was
changed.

**Interpretation:** this is a controlled schema repair, not result filtering. The
same 3×3 tasks and deterministic branch-selection procedure were retained.

**Claim impact:** pending formal corrected D.

## 2026-08-14 01:14 — Corrected formal Condition D

**Run ID:** `exp2_r4_condition_d_gripper_20260814T011457`

**Project Git SHA:** the run records the committed code immediately after
`6299208`; the current report-era predecessor SHA is
`e4966063c48c4ce122945c6cc643ed9f8060304f` after the subsequent R5 implementation.

**Exact command:**

```powershell
'C:\Users\Guoji\anaconda3\envs\libero-exp1\python.exe' scripts\exp2\validate_zero_twins.py --run-id exp2_r4_condition_d_gripper_20260814T011457 --reference-run runs\exp2_r2_gripper_refs_20260814T011336 --repeats 3 --condition-d-only
```

**Config:** original frozen thresholds; final gripper-complete 108-branch manifest;
3 repeats; Condition D only; no stochastic seed.

**Coverage:** 9 demonstrations; 12 branches per demo; 324 pairs; 17,121 paired
continuation steps; 222,573 component rows.

**Metrics:** final-success agreement 100%; all states finite; integration, qpos,
qvel, controller, EEF, and terminal object-pose median/P95/max all `0.0`. All
bootstrap 95% intervals were `[0.0, 0.0]`. Contact/non-contact, early/middle/late,
and all three tasks were separately exactly zero. Every gate criterion passed.

**Warnings / exceptions:** common nonfatal runtime warnings only; no exception,
failed assertion, NaN, or Inf. `failure_examples.json` is empty.

**Prefix diagnostic:** maximum recreated-prefix INTEGRATION L2
`2167.4243431321693`; legacy L2 `1.2156529760717303`. This is not a twin failure
because both twins are restored from the same saved D snapshot, but it shows that
public-prefix reconstruction remains non-equivalent.

**Interpretation:** corrected D is the smallest tested passing condition. C failed;
additional explicit runtime history is necessary in this stack. The exact repair
strongly supports the gripper integrator as the decisive omitted state.

**Alternative explanations:** another field that happens to covary perfectly with
gripper history is logically possible, but adding only the audited gripper field to
the previously tested schema changed the complete formal result from failure to
exact identity without changing tasks, branches, repeats, or thresholds.

**Claim impact:** allowed—accurate policy-step branching in the tested stack
requires INTEGRATION plus explicit controller/robot/environment/gripper state.
Temporal decision sparsity remains untested.

**Next experiment:** R5 was legally reached; run only the fixed 48-intervention
measurability smoke test.

## 2026-08-14 01:26 — R5 q-intervention measurability smoke

**Run ID:** `exp2_r5_q_smoke_20260814T012633`

**Project commit / files changed:** `e496606` added the gated smoke implementation,
`configs/perturbation_smoke.json`, q-intervention masking utilities, joint-limit
discovery, raw Parquet output, plots, and tests.

**Exact command:**

```powershell
'C:\Users\Guoji\anaconda3\envs\libero-exp1\python.exe' scripts\exp2\perturbation_smoke.py --run-id exp2_r5_q_smoke_20260814T012633 --zero-twin-run runs\exp2_r4_condition_d_gripper_20260814T011457 --reference-run runs\exp2_r2_gripper_refs_20260814T011336
```

**Exact config:** `demo_0` for all three tasks; frozen quantiles 0.15, 0.35, 0.65,
and 0.85; two PCG64 normal unit directions per branch; seed `20260814`; both signs;
epsilon fraction `0.005` of verified runtime joint range; q-only masking; matched
zero controls. Exact branch indices were drawer 21/48/89/116, stove 12/28/51/67,
and bowl 13/31/58/76.

**Metrics:** 48 interventions, 2,488 per-step rows, 16 interventions per task, 24
per sign. All zero controls passed; global zero-noise P99 and maximum were `0.0`.
Maximum non-arm INTEGRATION-component Linf at intervention was `0.0`. All q values
were within audited joint limits and all future states were finite. All 48 effects
exceeded 10 times their corresponding zero-noise P99. Maximum-future INTEGRATION
effect minimum/median/approximate empirical P95/maximum was
`32.83229142722571 / 49.824963778807955 / 2858.992845159297 /
4405.331419219012`. Five interventions flipped final task success. Summed contact
and predicate divergence steps were 904 and 130. Maximum terminal object-pose L2
was `0.4943669282463729`. Gate passed.

**Warnings / exceptions:** no gate exception, no invalid q, no NaN/Inf, no branch
removal. Common robosuite/Gym warnings were retained in stderr.

**Interpretation:** the intervention magnitude is measurable above restoration
noise. The large spread and five flips motivate a formal time-indexed experiment,
but the smoke sample is not designed to estimate sparsity or event alignment.

**Alternative explanations:** early branches have longer continuations, raw
INTEGRATION norms mix components, and task/contact dynamics can amplify small
changes. EXP3 must use fixed-horizon and component/task outcomes with
demonstration-level inference.

**Claim impact:** allowed—local q interventions can be measured above the matched
zero-twin noise floor. Forbidden—decision sparsity, contact alignment, or latent-RL
benefit.

**Next experiment:** preregister the full 108-branch causal criticality experiment
described in `reports/next_exp_from2.md` before inspecting more q outcomes.

## Final verification and report handoff

**Commands:**

```powershell
'C:\Users\Guoji\anaconda3\envs\libero-exp1\python.exe' -m pytest -q
'C:\Users\Guoji\anaconda3\envs\libero-exp1\python.exe' -m pip check
```

**Tests:** 25 passed, 0 failed, 0 skipped. Dependency check: no broken requirements.

**Artifact audit:** full A–D Parquet rows were 1,296 pairs / 68,484 steps / 890,292
components; corrected D was 324 / 17,121 / 222,573; R5 was 48 summaries / 2,488
steps. All five required plots and all run provenance files exist.

**Final strongest claim:** within the tested stack and pilot, corrected D provides
exact same-runtime matched branching, and fixed local q interventions are measurable
above its exact zero floor.

**Claims still forbidden:** temporal decision sparsity, contact-aligned decision
bottlenecks, event-driven RL justification, and latent-action superiority.

**Single next action:** commit an outcome-blind EXP3 preregistration and execute the
full time-indexed criticality design without changing epsilon or selecting branches
from R5 outcomes.

## EXP3 T0 — substrate audit and scaffold

**Run:** `runs/exp3_t0_audit_20260814T020528`

**Actions:** read the complete EXP3 protocol and required project/EXP1/EXP2 records;
inspected the exact corrected-D restore implementation, reference manifest, branch
manifest, BDDL goals, and LIBERO predicate source. Added an isolated EXP3 scaffold,
configuration, metric helpers, and deterministic known-vector tests. No intervention
outcomes were generated or inspected.

**Gate result:** PASS. The frozen reference gate and corrected Condition D both pass;
there are nine reference trajectories and twelve unique branches per trajectory.
Input hashes were captured in the immutable run. The audit detected an NVIDIA GeForce
RTX 4090 (driver 596.49); MuJoCo remains on the validated CPU execution path.

**Verification:** 30 tests passed; the prior 25-test baseline remains intact and five
new EXP3 metric tests pass.

**Claim impact:** none. This is provenance and implementation preparation only.

**Next action:** commit the scaffold, then run the corrected-D T1 regression before
freezing the outcome-blind EXP3 manifests.

## EXP3 T1–T4 — corrected-D regression and protocol freeze

**Runs:** failed sandbox provenance `runs/exp3_t1_condition_d_20260814T020559`;
formal passing run `runs/exp3_t1_condition_d_20260814T020613`.

**T1 result:** PASS. All 324 matched pairs across 108 branches and three repeats
completed (17,121 continuation steps; 222,573 atomic-component rows). Integration,
q, qvel, controller/robot/gripper state, EEF, terminal object pose, and final success
errors were exactly zero, including task, phase, and contact-event strata. The first
attempt was blocked only because robosuite writes a fixed `C:\tmp\robosuite.log`; it
was preserved and the experiment was rerun with approved filesystem access.

**T2–T4 freeze:** wrote the nine required outcome-blind manifests plus a manifest
hash index. The new PCG64 master seed is `4223443001079176503`, deterministically
derived from the scaffold commit and frozen branch-manifest hash; it is independent
of the EXP2 R5 seed. All 432 raw/unit directions and both signs were frozen, yielding
864 interventions, and all were prevalidated against audited Panda joint limits.
The primary endpoint is the remaining-horizon mean of six separately normalized
physical components, followed by the median across eight interventions per branch.
Top-20 mass is the preregistered decision statistic.

**Verification:** 31 tests passed, including exact regeneration of every frozen
PCG64 direction and known 12-point concentration/all-zero vectors. `pip check` found
no broken requirements.

**Claim impact:** none; no formal q outcome has been generated under EXP3.

**Next action:** commit the frozen protocol, perform a non-scientific dry run, then
execute the all-branch two-zero-continuation gate.

## EXP3 dry run and first T5 attempt

**Runs:** `runs/exp3_tdry_20260814T021501` (PASS) and
`runs/exp3_t5_zero_20260814T021530` (STOPPED by gate).

The dry run completed one branch, two signed interventions, and 262 effect rows with
zero non-arm mutation. The first formal zero attempt completed all 108 branches and
5,707 reference steps. Integration, q, qvel, EEF position, object position, contacts,
predicate, success, and finiteness were exact. The gate stopped because geodesic
helpers returned angles up to `1.13484e-7` rad for bitwise-identical rotation matrices
and `2.98023e-8` rad for identical quaternions: normalizing or multiplying a slightly
non-orthonormal representation before `acos` introduced roundoff.

This is a metric implementation defect, not simulator divergence or an outcome-based
protocol change. Added an exact-equality short circuit to both geodesic functions and
regression tests using identical drifted representations. No formal q sweep was
started. The failed run remains immutable.

**Next action:** commit the numerical fix and rerun the complete T5 gate under a new
run ID.

## EXP3 corrected T5 — all-branch matched-zero gate

**Run:** `runs/exp3_t5_zero_corrected_20260814T021800` — PASS.

All 108 frozen branches produced two independent zero continuations and 5,707 saved
reference steps. Maximum discrepancy was exactly zero for integration state, arm q,
arm qvel, EEF position/orientation, task-object position/orientation, raw contact
count, contact-pair set, task predicate, and terminal success. All states were finite.

The original frozen scientific design, directions, normalizers, and thresholds remain
unchanged. The corrected run is the sole matched-zero source authorized for the full
864-intervention sweep.

**Next action:** execute all four directions and both signs at all 108 branches, with
no branch removal or post-outcome tuning.

## EXP3 T6–T13 — full sweep, inference, and disposition

**Formal run:** `runs/exp3_t6_full_20260814T022200` — PASS.

All 864 interventions and 45,656 continuation steps completed. Every one of 108
branches had four directions and two signs; maximum non-arm integration mutation was
zero; all joint limits and finiteness checks passed; no branch was removed. There
were 58 terminal-success flips (10 drawer, 48 bowl, 0 stove).

**Primary result:** demonstration-median top-20 effect mass = 0.449319, hierarchical
bootstrap 95% CI `[0.357858, 0.586139]`, compared with uniform 0.25. This passes the
frozen partial-support concentration criterion. It fails strong support because
task-median cross-demo Spearman values were only 0.2727/0.4061/0.1879, only 2/9 demos
were direction/sign stable, and minimum LODO top-20 mass was 0.4381.

**Direction result:** median same-direction opposite-sign rho = 0.6329, but median
direction-only rho = 0.3776 and all-pair rho = 0.2343. Time criticality is materially
direction-dependent.

**Event result:** contact and gripper events were not enriched. Predicate-first-true
had nominal unadjusted ratio 1.332 and p=0.019, but failed BH (`q=0.057`) and the
remaining-horizon/phase/demo-adjusted permutation test (`p=0.119`, `q=0.358`). No
event mechanism is supported.

**Outcome relevance:** primary criticality had Spearman 0.0785 with terminal object
effect and 0.2660 with predicate-divergence fraction. Success flips demonstrate
behavioral consequence but do not validate a universal time ranking.

**Formal classification:** PARTIAL SUPPORT. Allowed claim: fixed small q effects are
temporally non-uniform in this pilot. Forbidden claims: strong replicated sparse
decision times, direction invariance, event-triggered mechanism, sparse-control
benefit, or latent-RL superiority.

**Verification:** 32 tests passed; `pip check` reported no broken requirements. All
twelve required plots and all required Parquet/JSON/manifests exist. Reports created:
`reports/exp3_report.md` and `reports/next_exp_from3.md`.

**Next experiment:** held-out demos 3–9, eight structured/held-out directions, and
outcome-blind task-progress alignment. Do not begin sparse control or RL until that
replication gate passes.

## EXP4 E4-0 to E4-3: held-out substrate, progress audit, and references

Read the complete EXP4 execution protocol, project direction, EXP1-EXP3 execution
documents, EXP1-EXP3 reports, `next_exp_from3.md`, evidence table, current research
log, and the exact EXP2/EXP3 snapshot, branching, intervention, manifest, and analysis
code before implementing EXP4.

**Baseline checks:** each selected HDF5 dataset contains 50 demonstrations, so exact
indices 3-9 are available without substitution. CUDA audit found PyTorch
`1.11.0+cu113`, CUDA available, NVIDIA GeForce RTX 4090, driver 596.49, and 24,564
MiB total memory. `pip check` passed. The first baseline pytest invocation reported
31 passes and one fixture error because the explicitly selected basetemp parent did
not exist; after creating that task-local parent, the scaffold suite passed 37/37.

**Runtime progress audit attempts:**

- `exp4_e4_0_1_runtime_audit_20260814` failed because the outer `ControlEnv` wrapper
  does not expose `object_sites_dict`; the inner environment does.
- `exp4_e4_0_1_runtime_audit_corrected_20260814` resolved drawer/stove state but
  failed because scanned bowl and plate objects do not expose generic bottom/top
  sites. No such site was assumed afterward.
- `exp4_e4_0_1_runtime_audit_geom_20260814` passed after switching bowl/plate height
  to the union AABB of active runtime collision primitives using geom pose, type, and
  size. The exact drawer joint is `wooden_cabinet_1_middle_level` (joint id 14,
  qpos address 38, range [-0.16, 0.01], Open threshold q < -0.14). The exact stove
  joint is `flat_stove_1_button` (joint id 16, qpos address 40, range
  [-0.005, 2.1], TurnOn threshold q >= 0.5).

**Held-out references:** `exp4_e4_3_heldout_refs_20260814` passed. All 21 exact
references (3 tasks x demos 3-9) finished successfully, all snapshots were finite,
and maximum integration and controller round-trip errors were exactly zero. Action
counts were drawer 121-158, stove 77-96, and bowl 80-105. Every pre-policy boundary
stores task progress features in addition to the corrected EXP2 snapshot.

**Branch candidates:** `exp4_e4_2_branch_candidates_20260814` passed with 21
trajectories, 12 unique outcome-blind branches each, and 252 total branches. Branch
selection is the unchanged EXP2/EXP3 ten-quantile plus contact/gripper audit rule.

**Corrected-D regression:** `exp4_e4_2_condition_d_20260814` completed all 756 pairs
(252 branches x 3 repeats), 39,714 continuation steps, and 516,282 atomic component
rows. Every measured numerical discrepancy was exactly zero, including integration,
q/qvel, controller, EEF, terminal object pose, task/phase/contact strata, and final
success. The reused EXP2 summarizer returned failure only because it hard-codes an
obsolete `all_nine_demonstrations` criterion. The immutable raw evidence was audited
by `exp4_e4_2_condition_d_reconciled_20260814`, which replaced only the cohort-count
criterion with exact EXP4 coverage (21 demos, 252 branches, 756 pairs); the reconciled
gate passed and retains hashes of the original metrics and Parquet files.

**Claim impact:** none yet. No EXP4 q-intervention outcome has been generated or read.

**Next action:** commit the tested EXP4 scaffold, freeze all 14 outcome-blind
manifests against that commit SHA, then run dry and formal matched-zero gates.

## EXP4 E4-4 to E4-9: formal outcome-blind freeze

Committed the tested scaffold as `33e7075c14ebc29de04ae13e301c7aa5dd0fd8cd`.
The preflight freeze generated the expected structure without q outcomes. The formal
freeze then wrote 14 required manifests plus the hash index under
`experiments/exp4_replicated_progress_criticality/manifests`, all bound to scaffold
SHA `33e7075`.

The frozen PCG64 master seed is `129054049736580372`, distinct from EXP3. Exactly
252 reproducible 7x7 bases were generated by Gaussian QR with determinant +1, plus
one held-out PCG64 random direction per branch. The maximum orthogonality error in
preflight was `1.11022302462516e-15`; all 2,016 directions and both signs passed
joint-limit checks, yielding exactly 4,032 planned interventions. The basis order is
randomized and stored per branch.

The primary is sign-paired `S_RMS` across the seven basis directions. The held-out
random direction is excluded from the primary. Physical-progress interpolation uses
the first crossing along original temporal order, never sorting nonmonotone progress.
The exact EXP3 normalizers and historical six-channel intervention metric are retained
for numerical comparability. The finite-difference operator uses only a signed,
normalized physical output vector; raw integration state remains diagnostic.

**Claim impact:** none. No EXP4 q-intervention outcome has been generated or read.

**Next action:** commit the frozen manifests, run the non-scientific dry path, then
execute the complete 252-branch two-continuation matched-zero gate.

## EXP4 E4-10/E4-11: dry path and formal zero gate

**Dry run:** `exp4_e4_10_dry_20260814` passed with one branch, one basis direction,
both signs, two interventions, 250 per-step effect rows, and maximum non-arm mutation
zero. The signed physical output column round-tripped as an actual Parquet
`list<double>`. Shard merge and SHA-256 raw-artifact locking also passed. Dry outcomes
are excluded from every formal analysis.

**Formal zero run:** `exp4_e4_11_zero_20260814` passed all 252 held-out branches with
two independent zero continuations at each branch and 13,238 saved reference suffix
steps. Integration median, P95, and maximum were all exactly `0.0`; terminal object
pose P95 was `0.0`. All expected branches, continuation IDs, finite states, and
failure checks passed. Wall time was 253.8 seconds. No branch was removed.

**Claim impact:** none. This authorizes, but does not itself support conclusions from,
the preregistered 4,032-intervention sweep.

**Next action:** execute every frozen basis/random direction and both signs at all
252 branches, preserving one immutable shard per task/demo and locking merged raw
artifact hashes before analysis.

## EXP4 E4-12: complete intervention sweep

Run `exp4_e4_12_full_20260814` completed all 4,032 frozen interventions: 252
branches, seven orthonormal basis directions plus one held-out random direction, and
both signs. It produced 211,808 per-step rows in 2,037.38 seconds. Every signed
perturbation was finite and within joint limits, all 16 interventions were present at
every branch, no branch was removed, and maximum non-arm integration-state mutation
at the branch boundary was exactly zero. There were 417 final-success flips (drawer
183, bowl 213, stove 21).

Raw files were locked before analysis:

- interventions: `21bf8fe958303236c0321078f8a41a89eb3776e0678c999728e2c961f1458eb3`;
- per-step effects: `6016ae5bb73570948505a2f533f160e20955dd4985d2d057c81ceeff4cebd6c7`;
- zero controls: `8344175d2e2f96532087ce81958f922e24e69356120b3a374f6b82dcc9b59a3c`.

## EXP4 formal analysis and GPU equivalence

The first GPU analysis attempt stopped before results because PyTorch 1.11 lacks the
newer stable-argsort API. It was replaced by a tie-free exact ranking implementation.
The next equivalence audit failed because `torch.median` selects the lower middle
element for even samples whereas NumPy averages the middle pair. The implementation
was corrected to the explicit arithmetic median without changing a tolerance; the
failed audit remains as `gpu_cpu_equivalence_failed_bootstrap_median.json`.

The final RTX 4090 float64 audit passed with no fallback. Basis aggregation,
Gram/eigenspectrum, progress interpolation, 4,000-resample hierarchical bootstrap,
and 4,000 cluster permutations ran on GPU. Final CPU/GPU errors were at most
`3.47e-18` for aggregation, `2.22e-16` for concentration, `2.66e-15` for Gram
matrices, and `1.82e-14` for singular values; bootstrap and interpolation errors were
zero and rank/source indices were exact.

The primary held-out demo-median top-20 mass was 0.45067 with hierarchical 95% CI
`[0.43599, 0.55598]` versus uniform 0.25. The LODO minimum was 0.44913. Physical
progress improved median cross-demo Spearman by only 0.04372, CI
`[-0.03333, 0.17489]`, one-sided p=0.25494. Task progress medians were 0.75195
(drawer), 0.86623 (bowl), and 0.71738 (stove), compared with time medians 0.68485,
0.79394, and 0.58788.

Direction stability remained poor: 5/21 demos passed, and held-out random-direction
agreement was 0.43357. Basis sign asymmetry had median 0.18476 and P95 0.75264.
Operator spectral norm had median 23.61 and P95 142.70; the median leading-eigenvalue
share was 0.75489. Residual/interactions accounted for 87.44% of balanced variance.
Terminal object and predicate relevance were weak (rho 0.15328 and 0.29648).
Contact, gripper, and predicate events all failed adjusted FDR testing.

A Parquet schema audit detected that ICC/Jaccard columns had been omitted because
the first record lacked those optional fields. Explicit null fields were added and
analysis reran against unchanged raw hashes. The independent output auditor then
passed exact counts, 14 manifests, 18 plots, 252 operators, raw hashes, GPU no-
fallback, and an empty failure list. Visual plot QA passed. The final test suite
passed 42/42 and `pip check` passed.

**Formal classification:** `replicated_nonuniformity_without_aligned_sparse_times`.
Temporal non-uniformity replicated, but the stronger preregistered sparse-time rule
failed five of seven requirements. The permitted claim excludes universal sparse
decision times, a stable global q direction, event triggers, and sparse-policy
benefit.

**Next action:** EXP5 should cross-fit reference-only physical state matching and
test basis-invariant local response subspaces at radii 0.0025 and 0.005 (with a
stratified 0.01 calibration subset) on new demos 10–19 before any sparse or latent
policy training.

## EXP5-0: cohort availability and baseline

Read the complete EXP5 protocol and required prior project/evidence sources. The
requested EXP4 experiment summary file is absent, but the controlling EXP4 prompt,
14 manifests, report, code, raw hash manifest, and run evidence are present. Existing
user changes to root/report READMEs and prompts were left untouched.

`exp5_s0_availability_20260814` passed: all 21 development and 30 confirmatory HDF5
episodes exist, have matching states/actions lengths, valid lengths, and embedded
model XML. Each selected task has 50 dataset episodes. PyTorch 1.11.0+cu113 detected
an RTX 4090. `pip check` passed. The first pytest call had 40 passes and two fixture
errors because its explicit `.tmp` parent did not exist; after creating it, 47/47
tests passed.

Pre-run expectation was that all 30 historical successful demonstrations would also
produce successful same-runtime local references, based on EXP4 demos 3–9.

## EXP5 confirmatory reference attempts and hard stop

The first run, `exp5_s4_confirmatory_refs_20260814`, failed before simulator import
because sandbox access to robosuite's fixed `C:\tmp\robosuite.log` was denied. The
approved corrected monolithic run,
`exp5_s4_confirmatory_refs_corrected_20260814`, exceeded the outer execution-session
duration and ended without a metrics file after partial drawer output. Neither run
contributed scientific evidence.

Five immutable two-episode shards were then executed. Runs for demos 10–11, 12–13,
14–15, and 18–19 passed. Run `exp5_s4_refs_16_17_20260814` completed but failed its
success gate because `open_the_middle_drawer_of_the_cabinet/demo_17` was unsuccessful.
All 30 references were generated; 29 succeeded. Per task: drawer 9/10, stove 10/10,
bowl 10/10. Every snapshot was finite and maximum integration/controller round-trip
errors were zero.

Read-only diagnosis found that the failed local continuation's audited middle-drawer
joint never moved beyond -0.021819, whereas the historical HDF5 trajectory reaches
-0.158130 and records reward/done on its last step. The exact Open threshold is
q<-0.14. Historical/local legacy divergence for the episode had median 0.04695, P95
2.25925, and maximum 5.27784.

The exact protocol requires all demos 10–19 and forbids substitution, reuse, or task
replacement. EXP5 therefore stopped before descriptor/matcher freeze, corrected-D
branch validation, zero controls, q interventions, GPU formal work, or inference.
Interventions at every radius are zero. No formal EXP5 classification is legally
available, and the state-conditioned hypothesis remains untested.

The reference attempt occurred before the planned development descriptor/matching
freeze, a disclosed ordering deviation that introduced no q-outcome leakage. The
highest-value next action is an isolated drawer demo17 reference-reconciliation
experiment before redesigning any confirmatory cohort.

## EXP5 resumed and completed under the authorized cohort amendment

The user prospectively amended the reference rule after the original reference gate
and before any EXP5 q outcome: retain passing demos 10–19 and fill only a task
deficit with the first passing unused demo scanned from 20 upward. Drawer demo20 was
the first candidate and passed. Run `exp5_s4_confirmatory_refs_amended_20260814`
therefore passed with 30 references and exactly ten per task; drawer demo17 remains
preserved as a rejected negative reference.

The outcome-blind freeze used 5,385 reference states, five folds per task, 16
prototypes per task/fold, 480 branches, seven basis plus one held-out direction,
small/main radii on all branches, and radius 0.01 on 96 frozen branches. It produced
8,448 direction/radius rows and 16,896 signed interventions. Protocol and manifests
were committed as `e851236` before dry q outcomes.

GPU float64 equivalence passed on the RTX 4090. Tests passed 47/47 with a repository-
local pytest basetemp and pip check passed. Full matched-zero run
`exp5_s11_zero_full_20260814` passed 480/480 branches and 18,614 suffix steps;
integration median/P95/max and terminal-object P95 were all exactly zero.

Fifteen immutable q shards completed all 16,896 interventions and 658,288 future-
step rows: 7,680 small, 7,680 main, and 1,536 large. Every key matched the frozen
manifest, all arrays/q limits passed, maximum non-arm Linf was zero, and no branch
was removed. The merged raw run is `exp5_s12_s13_q_full_locked_20260814`; tracked
raw hashes were committed as `dd34ad3` before inference.

Final run `exp5_s14_s20_formal_analysis_frozen_matching_20260814` verified raw
hashes and enforced frozen development scaling, crossfit precision, monotone
matching, and rejection. State matching did not improve scalar replication: median
delta -0.03627, 95% CI [-0.27947, 0.03343], permutation p/BH q=0.9950. State rho
was 0.7576 Drawer, 0.2000 Bowl, and 0 Stove; top-1 similarities were 0.1968,
0.0906, and 0.0803.

Only 21/480 branches (4.375%) passed the local-linearity gate and only 1/30 demos
met the small/main top-1 threshold. Main/large top-1 similarity was 0.1617 and
spectral discrepancy 0.4369. Held-out rank prediction was the sole strong-rule pass
(rho 0.6265), but vector errors were too large.

**Formal classification:** `finite_radius_nonlinearity_dominates`. Oracle scheduling
and latent RL remain ineligible. The next action is a smaller-radius convergence and
empirical trust-region experiment.

## EXP6 — outcome-blind subset and numerical-calibration freeze

- Timestamp: 2026-08-14 (America/New_York)
- Research question: does the corrected-D finite-radius q response become stable below the EXP5 radius while remaining above the measured zero floor?
- Pre-run expectation: the exact deterministic runtime should resolve the primary 0.000625 radius; the optional 0.0003125 radius remains conditional on the frozen calibration gate.
- Runs: `exp6_s1_branch_subset_20260814`, `exp6_s2_calibration_freeze_20260814`
- Reference source: `exp5_s4_confirmatory_refs_amended_20260814` (30 passing same-runtime trajectories).
- Outcome-blind selection: 8 reference-only branches per demonstration, stratified by frozen time/progress/contact/gripper/predicate cost; no EXP5 q outcome was read.
- Coverage: 240 branches total; 134 reference target–gripper contact states and 106 non-contact states.
- Calibration freeze: 6 branches (2/task), five candidate radii, seven orthonormal plus one held-out direction, both signs, two repeats per signed intervention, four matched-zero repetitions.
- Planned calibration executions: 960 signed interventions.
- Tests: 51 passed; `pip check` clean at baseline.
- Claim impact: none yet; manifests were hashed before calibration outcomes.

## EXP6 — numerical calibration result and formal freeze

- Runs: failed sandbox-only `exp6_s3_calibration_zero_20260814`; passing `exp6_s3_calibration_zero_rerun_20260814`; `exp6_s4_calibration_interventions_20260814`; `exp6_s5_resolution_audit_20260814`; failed pre-freeze `exp6_s6_formal_freeze_20260814`; passing `exp6_s6_formal_freeze_rerun_20260814`.
- The first zero attempt was blocked before simulator execution because robosuite could not write `C:\tmp\robosuite.log`; it was rerun unchanged outside that sandbox restriction.
- Zero floor: exact 0 for all measured repeated-zero channels across 6 branches and 4 repeats.
- Calibration: 960/960 interventions, 56,960 future-step rows, all finite, maximum non-arm Linf 0, 46 terminal-success flips (diagnostic only).
- Repeatability: scalar, signed vector, operator, spectrum, and direction/sign rank discrepancies all exactly 0.
- Maximum q injection error: 2.21e-16. Minimum signal/resolution-constant ratio: 1.59e9.
- Radius decision: primary 0.000625 is resolvable; optional 0.0003125 also passes and is admitted before formal outcomes.
- Formal pre-freeze joint-limit audit stopped on Stove demo11 action 47 before writing manifests. A reference-only nearest-unused-boundary rule replaced action 47→46 and action 48→49; no q-response outcome entered the replacement rule. The failed freeze was preserved.
- Formal freeze: 240 branches, 5 radii, 9,600 unsigned direction rows, 19,200 signed interventions.
- Claim impact: calibration supports numerical resolvability only; it does not establish response convergence.

## EXP6 — GPU gate, dry run, formal zero gate, and sweep launch

- GPU run: `exp6_s7_gpu_equivalence_20260814`, RTX 4090 float64.
- GPU gate result: failed the frozen absolute Gram and eigenspectrum tolerances. Maximum discrepancies were 1.19e-7 and 1.79e-7 respectively; operator discrepancy was 9.09e-13 and projector discrepancy 5.27e-15. Thresholds were not relaxed post hoc.
- Consequence: GPU formal analysis stopped; CPU is the sole formal source of truth. MuJoCo physics remains CPU in all cases.
- Dry run: `exp6_s8_formal_dry_20260814`, 1 branch, all 5 radii, 80 interventions, 9,760 future-step records; passed with non-arm Linf 0.
- Formal zero run: `exp6_s9_formal_zero_20260814`, 240/240 branches, 9,682 reference future steps, all repeated differences exactly zero and every gate passed.
- Formal sweep: ten disjoint three-trajectory shards named `exp6_s10_formal_shard_00_20260814` through `exp6_s10_formal_shard_09_20260814`, launched concurrently only after the zero gate passed. Frozen total remains 19,200 interventions.
- Pre-run expectation: smaller adjacent radii may improve scale consistency, but EXP5's strong directional rotation makes the full H1/H2/H3 convergence rule uncertain.
- Analysis policy: no formal outcome is inspected until all shards pass, merge coverage equals the manifest exactly, and raw hashes are locked.

## EXP6 — locked formal result

- Raw run: `exp6_s11_formal_raw_locked_20260814`.
- Analysis run: `exp6_s12_formal_analysis_cpu_20260814`.
- Formal execution: 19,200 unique interventions, 774,560 per-step records, 1,200 operators, 960 adjacent-radius comparisons, 9,600 exact contact-mode pairs, and 542 success flips.
- Raw gate: exact manifest coverage; 3,840 interventions per radius; maximum non-arm Linf 0; maximum q injection error 2.22e-16; all finite; no branch deletion.
- H1: fail. 0/30 demos had primary-pair median top-1 >=0.80; hierarchical median 95% CI [0.3138, 0.5931].
- H2: fail. 89/240 branches (37.08%) had primary-pair spectral discrepancy <=0.20.
- H3: fail. Smallest-radius demo-median held-out rho 0.7381, but median vector error 0.7049.
- H4: pass. Contact-divergent convergence failure 100% versus 92% when preserved; demo-clustered difference +0.0778, 95% CI [0.0222, 0.1389], permutation p=BH q=0.01525.
- Trust regions: 7/240 resolved, all Bowl; 233 unresolved.
- Formal classification: `contact_mode_conditioned_convergence` under the frozen priority rule.
- Interpretation: exact contact switching adds a supported failure mechanism, but preservation is not sufficient; unconditional response remains strongly asymmetric and radius dependent.
- Eligibility: scheduler false; latent RL false.
- Output audit: 18/18 required artifacts, 16/16 plots, row counts, and copied raw hashes all passed.
- Literature scan (2026-08-14): Contact Trust Region (arXiv:2505.02291) motivates unilateral-contact-aware local regions; recent smooth differentiable contact work (arXiv:2602.20304 and arXiv:2601.22849) is retained only as a separately labeled diagnostic alternative; ARCH (PMLR 305, 2025) motivates later hybrid hierarchy only after mode prediction is independently validated.
- Next experiment: EXP7 hybrid contact-mode-conditioned, boundary-margin, short-horizon response-field validation. No control training.

## EXP8-0 to EXP8-4 — independent cohort and contact-frame audit

- Timestamp: 2026-08-14 (America/New_York)
- Research question: can continuous surface geometry and action-relative point kinematics explain cross-demonstration preserved-mode H1 operators?
- Pre-run expectation: nearest-point and tangent-frame fields should be exactly reproducible; cross-demo operator improvement is uncertain because EXP7 retained heavy branch-level tails.
- Baseline: 59/59 tests passed; `pip check` reported no broken requirements.
- Availability run: `exp8_s0_cohort_audit_20260814`; unused candidates were Drawer 19, Bowl 20, Stove 20.
- Reference runs: `exp8_s1_refs_drawer_31_40_20260814`, `exp8_s1_refs_bowl_30_39_20260814`, and `exp8_s1_refs_stove_30_39_20260814`; all 30 first candidates succeeded and passed finite corrected-D qualification. Merged cohort: `exp8_s2_independent_refs_20260814`.
- Contact identity run: `exp8_s3_contact_identity_audit_20260814`; exact observed relevant pairs were Drawer 12, Bowl 57, Stove 21.
- Preserved negative run: `exp8_s4_contact_frame_audit_20260814` stopped at the first Stove trajectory because the inherited 0.05 m `mj_geomDistance` query returned no segment for any far free-space registered pair. No q outcome existed.
- Corrected audit: `exp8_s4_contact_frame_audit_r1_20260814` used the prospectively fixed query ladder 0.05/0.2/1/5 m, never a body-center proxy. It restored 3,232 reference boundaries and 120 repeated boundaries. Maximum repeated contact-frame feature range was exactly zero; active-pair range was 0–16, valid-pair range 12–57, and 1,758 boundaries had positive audited `mj_contactForce` normal force.
- Force semantics: MuJoCo 3.2.3 `mj_contactForce` was treated as force:torque in the native contact frame, transformed into the deterministic gauge. It was not relabeled as impulse.
- Branch run: `exp8_s5_branch_selection_20260814`; 360 outcome-blind branches, 12 per demo, balanced over time, progress, mode, continuous gap, gripper, predicate, and pair support.
- GPU actually used: no; simulator and reference audits were CPU-only.
- Claim impact: none yet. No EXP8 q outcome or response operator has been generated or inspected.
- Next action: commit the audited scaffold, freeze all model/crossfit/statistical manifests, then run GPU equivalence and matched-zero gates.

## EXP8-5 to EXP8-8 — formal protocol freeze and GPU equivalence

- Project scaffold SHA: `82e428b`.
- Freeze output: `experiments/exp8_continuous_contact_frame/manifests`; 25 required manifests, 360 branches, 8,640 radius/direction rows, and 17,280 signed interventions. No joint-limit replacement was required.
- Model freeze: exact-mode/time/progress baselines, EXP5-state-plus-gap/normal-velocity Baseline B, and permutation-invariant contact-frame RBF kernel ridge Primary. Five outer demo folds and nested training-only hyperparameter selection were frozen.
- Risk freeze: training-fold threshold maximizes specificity subject to sensitivity at least 0.85; test-fold labels are forbidden for threshold selection.
- Support run `exp8_s6_frozen_contact_features_20260814` preserved a negative gate caused by an incorrect whole-branch no-contact definition of free-space support. The corrected pair-level run `exp8_s6_frozen_contact_features_r1_20260814` passed: every task had active contacts, inactive valid nearest pairs, multiple normal directions, and contact-age variation. Bowl has no wholly contact-free selected branch, so claims are limited to pair-level free-space support.
- GPU run: `exp8_s7_gpu_equivalence_20260814`, RTX 4090, CUDA 11.3, PyTorch 1.11.0, float64. All 11 preregistered components passed without fallback or tolerance relaxation. Maximum absolute discrepancy was 2.13e-14 for Gram matrices; kernel-ridge prediction discrepancy was 2.22e-15.
- Claim impact: none. These are support, numerical, and design gates only; formal q outcomes remain unobserved.
- Next action: commit/hash the frozen protocol, run a dry execution, then complete the 360-branch matched-zero gate.

## EXP8-8 to EXP8-10 — dry path, matched-zero gate, and formal launch

- Frozen protocol SHA: `3845347`.
- Dry run `exp8_s8_dry_20260814` passed with one branch, two signed interventions, 238 per-step rows, exact zero twins, and maximum non-arm mutation 0.
- Matched-zero run `exp8_s9_zero_controls_20260814` passed all 360 branches and 11,766 reference suffix steps in 243.77 seconds. Integration median/P95/maximum and terminal object pose P95 were all exactly 0. No failure was recorded.
- Formal execution was partitioned before outcomes into ten disjoint contiguous three-demonstration shards, `exp8_s10_formal_shard_00_20260814` through `exp8_s10_formal_shard_09_20260814`. The union is exactly the frozen 30-demo cohort; each completed demo writes atomically.
- Formal analysis code was written and tested before raw merge or q-outcome inspection. Exact-mode Baseline A matching uses reference normalized-time proximity only and is prohibited from selecting a candidate by response similarity.
- Pre-run expectation: continuous geometry may improve average H1 similarity, but the EXP7 heavy tail makes the H2 p90 gate and the stringent H1 +0.15 increment uncertain. The selective-risk threshold may improve specificity at a sensitivity cost.

## EXP8-10 to EXP8-14 — locked formal result

- All ten immutable formal shards completed and passed. Their union contained exactly 17,280 unique signed interventions, 564,768 per-step response rows, and 246 terminal-success flips. Maximum non-arm mutation was 0 and maximum q-injection error was 2.22e-16.
- Raw run `exp8_s11_formal_raw_locked_r1_20260814` matched the frozen manifest exactly. Four raw SHA-256 values were committed in `raw_hash_manifest.json` as commit `a56ea38` before any formal analysis.
- Assembly run `exp8_s12_horizon_assembly_20260814` verified the hashes and produced 69,120 horizon responses, 34,560 mode outcomes, 4,320 operator rows, and 4,320 held-out rows.
- Several failed analysis attempts were preserved. They exposed only implementation/schema issues before a final decision was written: nested Arrow arrays required explicit float64 decoding; H4 intent and conditional estimands required independent per-horizon fitting; collinear ridge folds required an equivalent augmented least-squares solve; and `direction_role` had to remain a merge key. Each repair was tested and committed without changing data, folds, features, gates, or hyperparameter grids.
- Final analysis run `exp8_s13_formal_analysis_r5_20260814` passed its output gate. Independent audit found all 20 required artifacts, all 16 required plots, valid raw hashes, exactly 360 zero controls, and exactly 17,280 interventions.
- H1 failed: only 23/30 demonstrations and 69/360 branches had full both-sign, all-basis one-step mode-preserved support. Demo-level mean improvement over the best frozen baseline was -0.32859, 95% CI [-0.44707, -0.22008]. Every task estimate was negative.
- Primary mean top-1 similarity was 0.39387 versus 0.50230 for Baseline B and 0.78506 for the per-row best frozen baseline. H3 incremental improvement over Baseline B was -0.06891, 95% CI [-0.19012, 0.03635], permutation p=BH q=0.84179.
- H2 failed: 23 supported demonstrations, demo-median held-out norm-rank rho 0, demo-median vector relative error 1.0, overall p90 1.0, p95 1.00002, maximum 1.74736.
- H4 mode preservation fell from 0.19167 at H1 to 0.15278 at H3, 0.14444 at H5, and 0.04444 for the remaining horizon. Coverage-adjusted similarity was 0.07549, 0.06529, 0.07023, and 0.01902 respectively.
- H5 failed: AUROC 0.64131 with demo-cluster 95% CI [0.59424, 0.68355], AUPRC 0.63532, ECE 0.12076, sensitivity 0.90319, specificity 0.19630, and false-safe rate 0.80370.
- Ablations did not identify positive incremental value. Removing the normal/tangent frame raised mean top-1 by 0.06914; removing action projection changed it by -0.00102, removing contact age by +0.01109, and removing force by +0.01051. All tested feature-removal improvements over Baseline B had confidence intervals crossing zero and permutation p>=0.733.
- Formal classification: `continuous_geometry_insufficient`. Strongest allowed claim: deterministic continuous contact-frame measurements are reproducible and well supported, but the frozen contact-frame local model does not yield a reusable cross-demo operator or an adequate selective mode-risk gate. Offline scheduler, online control, and latent RL remain ineligible.
- Next action: close the explicit reusable-local-operator mainline and test an action-conditioned hybrid forward distribution over contact transitions, signed physical responses, and wrench/contact evolution, with demo-level cross-fitting and no controller training.

## EXP9-0 — protocol intake and retrospective-stage authorization

- Timestamp: 2026-08-14 (America/New_York).
- Controlling protocol: `prompts/EXP9_CODEX_PROMPT_Action_Conditioned_Hybrid_Response_Distribution.md`.
- Required PROJECT/EXP1–EXP8 reports, EXP8 next-experiment recommendation, evidence table, and latest research-log evidence were read before implementation. The prompt-referenced EXP3 directory has the repository spelling `exp3_time_indexed_q_criticality`; the prompt-referenced EXP4 experiment summary is absent, so its complete report, controlling evidence, manifests, and run artifacts are the authoritative fallback. No missing result was invented.
- Stage boundary: EXP8 locked data are authorized only for retrospective feasibility, architecture selection, debugging, and power estimation. No new cohort or q intervention may begin unless a non-oracle retrospective model passes all three frozen Stage-A gates: specificity above 0.50 at sensitivity at least 0.85; hybrid response score better than Baseline B at H1 and H3; and non-degenerate continuous uncertainty.
- Pre-run expectation: adding short history and the frozen future action chunk may improve contact-event selectivity, but the EXP8 risk specificity of 0.1963 and severe held-out-response failure make the Stage-A gate uncertain. A feasibility stop is a valid and protocol-compliant outcome.
- User-owned dirty README/prompt files and inaccessible historical pytest-temp directories remain outside EXP9 edits.
- Next action: establish repository/environment baseline, inspect immutable EXP8 schemas and action-history availability, then freeze and execute only the retrospective Stage-A comparison.

## EXP9-A0 — baseline and first target-build schema stop

- Baseline: 76/76 repository tests passed in 2.06 s; `pip check` found no broken requirements. Targeted EXP9 pure-metric tests passed 6/6. PyTorch 1.11.0+cu113 detected the RTX 4090.
- Locked-source audit: 17,280 interventions, 564,768 per-step rows, 26-dimensional signed response, 57-dimensional Baseline-B feature, 275-dimensional contact-graph feature, complete 7-dimensional reference policy actions, and the frozen five-fold demo assignments are present. No EXP8 file was modified.
- Failed development run: `exp9_a0_hybrid_targets_20260814`. It stopped before writing targets or fitting models because three trajectory-start boundaries encode the initial `gripper_current_action` as one scalar while the other 3,229 boundaries encode two finger values. All other history fields have fixed dimensions.
- Prospective repair: encode that audited controller field as a fixed two-value vector, copying available values and zero-padding only the missing second initialization component. Padding remains explicit through the history validity mask. This changes no outcome, fold, branch, or intervention and does not overwrite the failed run.
- Next action: rebuild under a new run id, audit counts/prevalence/dimensions, then freeze the Stage-A model comparison.

## EXP9-A0 — completed target audit and response-schema reconciliation

- Passing target run: `exp9_a0_hybrid_targets_r1_20260814`; 30 demonstrations, 17,280 interventions, and 86,400 aligned targets across H1/H3/H5/H10/remaining. History is 5×61, future action is 5×7, Baseline-B hybrid input is 82-dimensional, graph input 340-dimensional, and temporal input 375-dimensional.
- Event support is adequate: H1 exact-mode preservation is 0.633 Drawer, 0.320 Bowl, and 0.478 Stove; H3 is 0.535, 0.134, and 0.269. Named-pair add/drop events are also common in every task. Stage A is not stopped for prevalence.
- Failed prefit run: `exp9_a1_a6_retrospective_models_20260814`. It stopped before fitting because the first sampled EXP8 response had 26 dimensions while Bowl has 32 dimensions. The existing EXP8 assembly uses the frozen `3*n` task-object rule: Drawer/Stove have 26 channels and Bowl has 32. This is not a malformed row.
- Prospective repair: retain the exact task-specific output dimensions and train/evaluate each task model only on its valid channels. Do not truncate Bowl and do not add stochastic padded channels to Drawer/Stove. The target audit is regenerated under a new run id so the dimensional metadata is explicit.
- Next action: rebuild the dimension-explicit target audit and run the unchanged four-family Stage-A comparison under a new immutable run id.

## EXP9-A1 — GPU determinism gate stop

- Dimension-explicit target run `exp9_a0_hybrid_targets_r2_20260814` passed with 26 response channels for Drawer/Stove and 32 for Bowl; all other counts/hashes match the prior passing target build.
- Failed model run: `exp9_a1_a6_retrospective_models_r1_20260814`. Baseline computations began, but the first neural CUDA matrix multiplication stopped because `torch.use_deterministic_algorithms(True)` requires `CUBLAS_WORKSPACE_CONFIG` on CUDA 11.3. No predictions, aggregate metric, or gate decision was written.
- Repair: set `CUBLAS_WORKSPACE_CONFIG=:4096:8` before Python starts. Architecture, seeds, folds, epochs, data, thresholds, and score definitions remain unchanged. The failed run is retained.
- Next action: execute the frozen comparison with the deterministic cuBLAS workspace under a new run id.

## EXP9-A1 — task-specific score-sampling stop

- Failed model run: `exp9_a1_a6_retrospective_models_r2_20260814`. Deterministic GPU training completed the Drawer folds and entered the Bowl task, then stopped before prediction lock because the linear-family Monte Carlo score sampler still allocated the Drawer/Stove response width (26) instead of Bowl's audited width (32).
- Repair: allocate score samples from the already frozen per-task `response_dim`. This is a pure shape correction; it changes no output selection, random seed, model, fold, threshold, or metric. No completed gate or prediction artifact exists in the failed run.
- Next action: rerun the complete frozen comparison under a new immutable run id.

## EXP9-A1–A6 — post-lock plotting stop

- Failed model run: `exp9_a1_a6_retrospective_models_r3_20260814`. All frozen model fits and the write-once prediction lock completed, but the run stopped during plot generation because the caller had already created the empty `plots` directory and the plotting function attempted to create it again without `exist_ok=True`.
- The partial run is not accepted as the Stage-A result even though its prediction hash exists. No metric was used to change architecture, thresholds, seeds, epochs, or gates.
- Repair: allow the plotting function to reuse the caller-created empty directory. The full model/prediction pipeline will be rerun under a new run id rather than mutating the failed run.
- Next action: complete the full retrospective comparison and artifact generation.

## EXP9-A1–A7 — locked retrospective result and protocol stop

- Passing target run: `exp9_a0_hybrid_targets_r2_20260814`.
- Passing model run: `exp9_a1_a6_retrospective_models_r4_20260814`.
- Independent audit: `exp9_a7_output_audit_20260814`, all criteria passed.
- Stage: retrospective development only; no EXP9 confirmatory cohort, zero control, q intervention, or simulator row was generated.
- Inputs: 5×61 reference history, 5×7 frozen future actions, 82-dimensional Baseline-B hybrid input, 340-dimensional graph input; task-specific signed response dimensions 26 Drawer/Stove and 32 Bowl.
- Population: 30 locked EXP8 demonstrations, 17,280 interventions, 86,400 five-horizon targets. Formal Stage-A scoring used H1/H3/H5 and retained all intent-to-perturb test rows; held-out random directions were excluded from fitting.
- Crossfit: five complete-demo folds per task. Training-only normalization, response scale, residual uncertainty, and risk thresholds. Test predictions were written once before aggregation: 259,200 unique model/intervention/horizon rows; SHA256 `b2ce4d6d486fcdadbbdd235c9abe08e9d185ea09c2402321001730054a941d8f`.
- Models: Baseline B; factorized hybrid; lifted hybrid linear; graph mixture (102,592 parameters); temporal sequence mixture (74,624 parameters). Neural candidates used seeds 190901/190902/190903 as an equal-weight ensemble, RTX 4090, float32 fitting, deterministic cuBLAS workspace, and float64 scores. No seed was cherry-picked.
- Continuous result: graph relative energy improvement versus B was +37.52% H1, +43.42% H3, +44.52% H5. Temporal was +42.70%, +47.91%, +49.33%. H1/H3 demo-difference bootstrap CIs were fully positive for both. Graph 90% marginal coverage was 0.925–0.928; temporal 0.936–0.945; uncertainty was non-degenerate.
- Risk result: graph H1/H3 sensitivity 0.8414/0.7946 and specificity 0.3047/0.3458; temporal 0.8385/0.8262 and 0.3024/0.3159. False-safe rates were 0.654–0.698. No model preserved sensitivity >=0.85 while exceeding specificity 0.50 at both H1 and H3.
- Other families: factorized response score was about 2% worse than B; lifted hybrid energy was about 9.1 versus B 4.68–5.01 and therefore failed strongly.
- Target limitation: exact events, signed physical response, signed gap, and normal-velocity deltas were identifiable. Locked EXP8 per-step raw lacks perturbation-side wrench/force trajectory, so no wrench target was invented.
- Training cost: graph 102.41 s, temporal 213.03 s, total wall 427.98 s. MuJoCo was not rerun.
- Frozen Stage-A gate: failed. `architecture_selection.json` records `new_cohort_authorized=false` and an empty selected-family list.
- Classification: `action_conditioning_insufficient` for Stage-A feasibility. This does not erase the continuous proper-score development signal; it means the joint hybrid/risk estimand is not viable enough to justify new collection.
- Claim impact: offline scheduler false; risk-only scheduler false; online control false; latent RL false. A learned latent may be studied only inside a new phase/macro-action trajectory-estimand experiment.
- Reports: `reports/exp9_report.md` and `reports/next_exp_from9.md`; evidence table updated without changing EXP1–EXP8 rows.
- Next action: EXP10 should first retrospectively test phase-conditioned multi-scale macro-action trajectory distributions, including explicit phase/action ablations and a new stop/go gate, before any new simulation.
- Final verification: 86/86 repository tests passed in 2.41 s; `pip check` clean; EXP9 compileall passed; Git diff check found no whitespace errors. User-owned README, prompt, legacy pytest-temp, and unrelated files remained unstaged.

## EXP10-A0 — intake, phase schema, and target assembly

- Controlling protocol: `prompts/EXP10_CODEX_PROMPT_Phase_Conditioned_Macro_Action_Trajectory_Response.md`.
- Read the required prior reports/evidence/logs, inspected Git state, and verified the baseline: 86/86 tests passed and `pip check` was clean.
- Audited a schema ambiguity before modeling: `physical_progress_clipped` in the EXP8 contact-frame table is task-native qpos/distance, not a shared normalized phase axis. Froze a reference-only P0–P6 schema using normalized reference time, sustained target-gripper contact, monotone dwell/hysteresis, and the exact task predicate.
- Failed run `exp10_a0_phase_macro_dataset_20260814`: final JSON aggregation used `(task, episode)` tuple keys. It stopped before a completed metrics record or any model fit. The repair changed only summary keys.
- Completed run `exp10_a0_phase_macro_dataset_r1_20260814`, then found that short end-of-trajectory coarse-regime sequences lacked an explicit padding mask. No model result existed. Added last-valid-regime right padding plus a validity mask.
- Passing dataset run: `exp10_a0_phase_macro_dataset_r2_20260814`: 30 demos, 17,280 interventions, task-specific response dimensions 26/32/26, explicit H5/H10/endpoint/terminal/coarse-regime targets, and six frozen manifests. No unavailable perturbation-side wrench was fabricated.

## EXP10-A1–A11 — multi-route modeling, locks, and stop

- Incomplete model run `exp10_a1_a6_multiroute_models_20260814` completed the continuous comparisons but stopped before prediction lock when the missing regime padding became active in the event route. It produced no accepted authorization. The same 23-method registry was rerun from scratch on r2 data.
- Passing model run: `exp10_a1_a6_multiroute_models_r1_20260814`, RTX 4090, three frozen CVAE seeds, five whole-demo folds, 23 methods, H5/H10, 481.31 s.
- Locked 794,880 trajectory rows at SHA256 `d589125a440d254df79eddc4099b0ad4eb0ce6bd9af05525e3e23a4dbe75d994` and 51,840 event/terminal rows at SHA256 `ae1b1ba2a26de3e14d5e646183cd053a086f88dda235861faa4c1f1bff339c08` before route aggregation.
- Track A failed: every chunk/history candidate was worse than the temporal reimplementation at both horizons. Track B failed: correct phase and phase experts worsened response scores; event AUROC CI lower was 0.641. Track C failed: latent-16/32 worsened scores, while latent-0 was collapsed with 0.128 coverage.
- Track D supplemental OR gate `exp10_a7_endpoint_route_20260814` locked 34,560 predictions at SHA256 `0334dde11724557a6ebe720419c02ffea56d7a77f6f87496afb7324a162767a1`; terminal classification failed and endpoint error worsened 20.67% versus temporal.
- Track E failed: teacher-free 10-step coarse-regime rollout AUROC 0.7219, sensitivity 0.8287, specificity 0.4762, false-safe 0.5238.
- Track F failed despite CVAE H5/H10 energy improvements of 46.10%/45.80%: 90% marginal coverage was only 0.621/0.627. Separate three-seed audit found energy SD 0.000272 H5 and 0.000080 H10, confirming reproducible underdispersion rather than a lucky seed.
- Exact protocol artifacts were materialized in `exp10_a9_protocol_artifacts_20260814`: six track prediction files, phase/transition/chunk support, route/ablation/seed tables, Stage-A report, and 16 required non-placeholder plots.
- Final audit `exp10_a10_final_output_audit_20260814` passed 15/15 checks. Final gates A–F were all false; Stage B was not authorized and no new simulator row was generated.
- Multi-axis classification: all six axes unsupported; summary `no_macro_scale_structure`. Offline decision allocation, a dedicated latent study, and latent RL remain ineligible.
- Final verification: 104/104 tests passed in 3.21 s; `pip check` clean; EXP10 compileall passed.
- Reports: `reports/exp10_report.md` and `reports/next_exp_from10.md`. Proposed EXP11 changes the intervention to paired structured action-chunk replacement, with real perturbation-side contact/wrench logging and pathwise conformal calibration; it does not add another state encoder or start control/RL.

## EXP11 — structured macro-action replacement causal response

Completed across 2026-08-14/15. Final summary classification: `predictable_but_not_sparse_macro_response`.

### Stage 0

- Audited robosuite `OSC_POSE` semantics from installed source: normalized arm channels clip to `[-1,1]`; translation/orientation scales are 0.05 m/0.5 rad; Panda gripper is a sign command and cannot be continuously interpolated.
- Built reference-only K=5/10/20 DCT, cubic-spline, smooth-pulse and leave-demo-out task×phase residual support audits from immutable EXP8 references.
- Authorized I-A analytic, I-B residual, I-C phase edit; I-E only where exact gripper transitions existed; I-D task-space was marked unavailable because no audited cross-task IK/progress mapping existed.
- Identified only episodes 41–49 as unused. Frozen 41–42 for calibration and 43–49 for formal, making Stage 2 an availability-limited 7-demo/task pilot.

### Stage 1 calibration

- Generated 6/6 successful references, all integration/controller round trips zero.
- Ran 24 branches, 1,116 replacements and 77,180 per-step records.
- Zero replay exact; wrist force/torque valid on 100% of rows.
- I-A and I-B showed amplitude-effect correlations 0.448 and 0.635; paired-sign asymmetry was small (0.030/0.039).
- I-E gripper timing was too weak. I-B Bowl exceeded the 10% clipping gate and was excluded there; I-A/I-C were retained broadly.

### Stage 2 formal

- Generated 21/21 successful independent references (episodes 43–49).
- Froze 84 branches and 1,232 replacements: I-A 840, I-B 224 on Drawer/Stove only, I-C 168.
- Ran 84,740 per-step response rows; 84/84 zero branches exact; wrench validity 100%.
- Taskwise execution re-audit used unsaturated mismatch plus clipped-chunk fraction. All unsaturated P95 values were zero. Bowl I-A failed at 17.5% clipping; all other selected task×family cells passed.
- Formal mean H10: I-A 0.0860, I-B 0.1745, I-C 0.1206. Nontrivial fractions were 75.36%, 92.86%, 76.19%, closing the near-zero-target failure mode of EXP9/10.

### Models and gates

- Complete-demo balanced five-fold outer split with nested demo-level calibration.
- Methods: mean/action-norm baselines, P-A ridge, P-B object-centric ridge, P-C switching experts, P-D GRU, P-E graph MLP, conditional generative audit, P-G terminal and coarse-regime heads, pathwise conformal.
- Final neural analysis used frozen seeds 111012/111013/111014 equally, deterministic CUDA, RTX 4090. GRU/graph had 11,444/9,620 parameters.
- P-E graph H10/H20 improvement 37.51%/38.70%, demo improvement CI [0.02921,0.04807], 3/3 tasks positive, path coverage 0.9172.
- Full trajectory route still failed because terminal endpoint P90 worsened 315.94%.
- Terminal success-flip AUROC 0.8251, CI [0.7243,0.9073]: classification branch passed.
- Coarse regime AUROC/AUPRC 0.9354/0.9158, sensitivity/specificity 0.9395/0.8355: prediction and selective gates passed.
- Generative route rejected: two-Gaussian residual component separation 0.0528 despite high kurtosis/BIC; heavy tails were not called multimodality.
- Decision sparsity failed. Top-20% mass was Drawer/Bowl/Stove 0.2809/0.3794/0.2559; 0/3 tasks passed the full gate. Held-out pulse ranking transferred but did not create concentrated consequence.

### Engineering and audit

- Added 25 EXP11 known-answer tests; final repository tests 129/129.
- Required 31 artifacts and 24 plots all present; raw hashes verified.
- Preserved failed/obsolete analysis runs as audit history. Final analysis is `exp11_s2_formal_analysis_r4_3seed_deterministic_20260814`; final output audit is `exp11_s3_final_output_audit_r2_3seed_20260815`.
- No scheduler, policy, MPC, VLA, online control or latent RL was run.

### Next step

EXP12 should test a compact calibrated causal macro-action predictive latent. It may use EXP11 raw only for development, then requires a genuinely new task/trajectory cohort for confirmation. Outcome-focused offline allocation may follow only after predictive-latent confirmation; sparse allocation and latent RL remain unauthorized.
